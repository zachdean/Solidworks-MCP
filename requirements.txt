# SolidWorks MCP Server Dependencies
# Author: Samsaam Ali Baig
# Updated: 2026-01-22

# MCP SDK
mcp>=1.0.0

# Async support is provided by the stdlib `asyncio` module (no package needed)

# Logging and utilities
python-dotenv>=1.0.0

# ===== NEW: Direct COM Automation =====
# pywin32 for proper COM automation with error handling
# Windows-only: never a hard requirement on macOS/Linux dev machines
pywin32>=306; sys_platform == "win32"

# For COM VARIANT types (ByRef parameters)
# pythoncom is included with pywin32
