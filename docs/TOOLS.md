# SolidWorks MCP Tool Index

GENERATED by `scripts/gen_tools_doc.py` from the tool registry (`solidworks_mcp/tools/registry.py`) -- do not hand-edit. Regenerate with `.venv/bin/python scripts/gen_tools_doc.py` and commit the result; `scripts/check.sh` runs `gen_tools_doc.py --check` and fails the build if this file is out of date.

111 tool(s) registered. Call `get_capabilities` at runtime for which of these are usable against the currently connected SOLIDWORKS install.

## `get_capabilities`

Report the connected SOLIDWORKS version, the project's minimum required version, and every registered tool with its required version and whether it is usable right now. Call this before calling an unfamiliar tool to check it will actually work against the connected SOLIDWORKS install.

- **Minimum release:** none -- exempt from the version gate
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `connect_solidworks`

Connect to SolidWorks. Launches if not running.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `get_solidworks_info`

Get SolidWorks installation information.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `create_new_part`

Create a new part document.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `create_new_assembly`

Create a new assembly document.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `open_document`

Open an existing SolidWorks document.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `filepath` | string | yes |  |

- **Dossier reference:** none found in `docs/api/`

## `save_document`

Save the active document.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `filepath` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `close_document`

Close the active document.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `save` | boolean | no | False |

- **Dossier reference:** none found in `docs/api/`

## `get_document_info`

Get information about the active document.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `list_open_documents`

List all open documents.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `new_drawing_from_template`

Create a new drawing document from a .drwdot template via ISldWorks::NewDocument. Falls back to auto-discovering a template when template_path is omitted.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `template_path` | string | no |  |
  | `paper_size` | string | no | A3 |
  | `orientation` | string | no | landscape |
  | `scale_num` | number | no | 1 |
  | `scale_denom` | number | no | 1 |

- **Dossier reference:** [`ISldWorks::NewDocument`](api/01-documents-and-sheets.md#isldworksnewdocument)

## `get_document_type`

Identify the active document's type (Part/Assembly/Drawing/...) via IModelDoc2::GetType.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** [`IModelDoc2::GetType`](api/01-documents-and-sheets.md#imodeldoc2gettype)

## `open_or_activate_document`

Open a document via OpenDoc6, or activate it via ActivateDoc3 if already loaded.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `filepath` | string | yes |  |
  | `read_only` | boolean | no | False |
  | `lightweight` | boolean | no | False |

- **Dossier reference:** none found in `docs/api/`

## `rebuild_document`

Rebuild the active document via ForceRebuild3 (force) or EditRebuild3 (incremental).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `force` | boolean | no | True |
  | `top_level_only` | boolean | no | False |

- **Dossier reference:** none found in `docs/api/`

## `save_drawing`

Save the active document via Save3 (in place) or SaveAs3 (filepath given), decoding swFileSaveError_e in the result message.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `filepath` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `export_pdf`

Export the active drawing to PDF via IModelDocExtension::SaveAs3 + IExportPdfData, decoding swFileSaveError_e in the result message. Verifies the output file actually exists on disk afterward and reports its size.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `output_path` | string | yes |  |
  | `sheets` | any | no | all |
  | `open_after` | boolean | no | False |
  | `keep_invisible_layers` | boolean | no | False |
  | `high_quality` | boolean | no | True |

- **Dossier reference:** [`IModelDocExtension::SaveAs3`](api/01-documents-and-sheets.md#imodeldocextensionsaveas3)

## `export_dxf_dwg`

Export the active drawing's sheet(s) to DXF/DWG via IModelDocExtension::SaveAs3 + swDxfOutputFonts/swDxfMultiSheetOption/swDxfVersion user preferences -- not IDrawingDoc::ExportToDWG2, which does not exist for drawings (see docs/api/05-export-and-layers.md). Verifies every expected output file on disk afterward and reports all paths and sizes.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `output_path` | string | yes |  |
  | `format` | string | no | dxf |
  | `sheets` | any | no | all |
  | `version` | string | no |  |
  | `map_file` | string | no |  |
  | `export_fonts_as` | string | no | geometry |
  | `multisheet` | string | no | single_file |

- **Dossier reference:** [`IModelDocExtension::SaveAs3`](api/01-documents-and-sheets.md#imodeldocextensionsaveas3)

## `export_edrawings`

Export the active drawing to eDrawings (.edrw) via IModelDocExtension::SaveAs3 + swEdrawingsSaveAsSelectionOption, for review-only stakeholders. Reports a clear message (data.addin_available) when the eDrawings add-in appears unavailable rather than a generic COM error.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `output_path` | string | yes |  |
  | `sheets` | string | no | all |

- **Dossier reference:** [`IModelDocExtension::SaveAs3`](api/01-documents-and-sheets.md#imodeldocextensionsaveas3)

## `get_custom_properties`

Read all custom properties (document-level or a named configuration).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `configuration` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `set_custom_properties`

Add or overwrite custom properties (document-level or a named configuration).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `properties` | object | no | {} |
  | `configuration` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `batch_export_pack`

Export every deliverable for the active drawing in one call, so a caller doesn't have to orchestrate N per-sheet/per-format calls to export_pdf/export_dxf_dwg/export_edrawings itself. Continues past a per-file failure (recorded in the returned manifest and in a manifest.json written to output_dir) rather than aborting the whole batch; overall success is False if any file failed. filename_pattern tokens: {drawing} (document title), {sheet} ('all' unless per_sheet=True), {index} (1-based sheet position, 0 unless per_sheet=True), {date} (YYYY-MM-DD), {rev} (the 'Revision'/'Rev' custom property, or ''). Each token value is sanitized for the filesystem before use.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `output_dir` | string | yes |  |
  | `formats` | array | no | ['pdf'] |
  | `per_sheet` | boolean | no | False |
  | `filename_pattern` | string | no | {drawing}_{sheet} |
  | `include_native` | boolean | no | True |
  | `rebuild_first` | boolean | no | True |
  | `overwrite` | boolean | no | False |

- **Dossier reference:** none found in `docs/api/`

## `add_sheet`

Create a new drawing sheet via IDrawingDoc::NewSheet4. paper_size selects a swDwgPaperSizes_e landscape size (A/B/C/D/E/A0-A4), or 'custom' to size the sheet from width/height instead.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `name` | string | yes |  |
  | `template_path` | string | no |  |
  | `paper_size` | string | no | A3 |
  | `scale_num` | number | no | 1 |
  | `scale_denom` | number | no | 1 |
  | `first_angle` | boolean | no | False |
  | `width` | number | no |  |
  | `height` | number | no |  |

- **Dossier reference:** [`IDrawingDoc::NewSheet4`](api/01-documents-and-sheets.md#idrawingdocnewsheet4)

## `activate_sheet`

Make a sheet active via IDrawingDoc::ActivateSheet. Fails with the available sheet names if the given name doesn't exist.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `name` | string | yes |  |

- **Dossier reference:** [`IDrawingDoc::ActivateSheet`](api/01-documents-and-sheets.md#idrawingdocactivatesheet)

## `list_sheets`

Enumerate every sheet in the active drawing via IDrawingDoc::GetSheetNames, plus each sheet's scale, paper size, projection type, dimensions, and view count via ISheet::GetProperties2/GetViews.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** [`IDrawingDoc::GetSheetNames`](api/01-documents-and-sheets.md#idrawingdocgetsheetnames), [`ISheet::GetProperties2`](api/01-documents-and-sheets.md#isheetgetproperties2)

## `get_active_sheet`

Report the currently active sheet's name, scale, paper size, projection type, and dimensions via IDrawingDoc::GetCurrentSheet.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** [`IDrawingDoc::GetCurrentSheet`](api/01-documents-and-sheets.md#idrawingdocgetcurrentsheet)

## `set_sheet_properties`

Update a sheet's setup (paper size, template, scale, projection, custom dimensions) via IDrawingDoc::SetupSheet5. Reads the sheet's current setup first and only overrides the fields supplied here -- any field left out keeps its current value rather than being reset.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `sheet_name` | string | no |  |
  | `paper_size` | string | no |  |
  | `template_path` | string | no |  |
  | `scale_num` | number | no |  |
  | `scale_denom` | number | no |  |
  | `first_angle` | boolean | no |  |
  | `width` | number | no |  |
  | `height` | number | no |  |

- **Dossier reference:** [`IDrawingDoc::SetupSheet5`](api/01-documents-and-sheets.md#idrawingdocsetupsheet5)

## `set_sheet_scale`

Convenience wrapper over set_sheet_properties that updates only a sheet's scale via IDrawingDoc::SetupSheet5, leaving every other field (paper size, template, projection, dimensions) untouched.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `scale_num` | number | yes |  |
  | `scale_denom` | number | yes |  |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IDrawingDoc::SetupSheet5`](api/01-documents-and-sheets.md#idrawingdocsetupsheet5)

## `get_sheet_properties`

Read back a sheet's name, paper size, scale (numeric pair and a readable ratio string), projection angle, dimensions, and template info via ISheet::GetProperties2.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`ISheet::GetProperties2`](api/01-documents-and-sheets.md#isheetgetproperties2)

## `copy_sheet`

Duplicate a sheet, optionally N times. WORKAROUND: SolidWorks has no direct CopySheet API -- this selects the sheet, calls IModelDoc2::EditCopy, then IDrawingDoc::PasteSheet (inserted at the end of the sheet list, so tab order matches the returned creation order), verifying the sheet count actually increased after each copy rather than trusting PasteSheet's return value alone. new_name is only valid with count=1 since PasteSheet auto-names each copy (e.g. 'Sheet1(2)') and this tool doesn't guess a naming pattern for multiple copies.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `source_sheet` | string | yes |  |
  | `new_name` | string | no |  |
  | `count` | integer | no | 1 |

- **Dossier reference:** [`IModelDoc2::EditCopy`](api/01-documents-and-sheets.md#imodeldoc2editcopy), [`IDrawingDoc::PasteSheet`](api/01-documents-and-sheets.md#idrawingdocpastesheet-sheet-copy-workaround-no-direct-copysheet-api)

## `delete_sheet`

Remove a sheet. WORKAROUND: SolidWorks has no direct DeleteSheet API -- this selects the sheet via IModelDocExtension::SelectByID2 and deletes it via IModelDocExtension::DeleteSelection2, then re-reads the sheet list to confirm the sheet is actually gone rather than trusting DeleteSelection2's return value alone. Refuses to delete the last remaining sheet, making no COM call in that case.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `name` | string | yes |  |

- **Dossier reference:** [`IModelDocExtension::SelectByID2`](api/03-annotations.md#imodeldocextensionselectbyid2), [`IModelDocExtension::DeleteSelection2`](api/02-views.md#imodeldocextensiondeleteselection2)

## `rename_sheet`

Rename a sheet via ISheet::SetName. Rejects a name collision with an existing sheet before calling COM, and re-reads the sheet list afterward to confirm the rename actually took effect -- SetName has no return value of its own to check.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `old_name` | string | yes |  |
  | `new_name` | string | yes |  |

- **Dossier reference:** [`ISheet::SetName`](api/01-documents-and-sheets.md#isheetsetname-sheet-rename-paired-with-isheetgetname)

## `insert_model_view`

Place a model view on a drawing sheet via IDrawingDoc::CreateDrawViewFromModelView3. view_name accepts Front/Top/Right/Left/Bottom/Back/Isometric/Dimetric/Trimetric/Current (case-insensitive), mapped to the *Name form SolidWorks expects. Returns the created view's name for later view/annotation tools to target.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `model_path` | string | yes |  |
  | `view_name` | string | no | *Front |
  | `x` | number | no | 0 |
  | `y` | number | no | 0 |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IDrawingDoc::CreateDrawViewFromModelView3`](api/02-views.md#idrawingdoccreatedrawviewfrommodelview3)

## `insert_standard_3_view`

Insert the standard three-view set via IDrawingDoc::Create3rdAngleViews2 (ANSI, default) or Create1stAngleViews2 (ISO, first_angle=True). Snapshots and restores the swAutomaticScaling3ViewDrawings user preference around the call.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `model_path` | string | yes |  |
  | `first_angle` | boolean | no | False |
  | `auto_scale` | boolean | no | True |

- **Dossier reference:** [`IDrawingDoc::Create3rdAngleViews2`](api/02-views.md#idrawingdoccreate3rdangleviews2)

## `insert_projected_view`

Project a new drawing view off an existing one via IDrawingDoc::CreateUnfoldedViewAt3 -- the API's real name for the UI's 'Insert Projected View' (no *Project*-named method exists on IDrawingDoc/IView). Selects parent_view_name first, then projects in the given direction. Cardinal directions (up/down/left/right) stay orthographically aligned to the parent; diagonals break alignment to be freely positioned off-axis. Inherits the parent's scale -- nothing here changes it. offset moves the new view to an exact distance from the parent afterward via the IView::Position setter.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `parent_view_name` | string | yes |  |
  | `direction` | string | yes |  |
  | `offset` | number | no |  |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IDrawingDoc::CreateUnfoldedViewAt3`](api/02-views.md#idrawingdoccreateunfoldedviewat3), [`IView::Position`](api/02-views.md#iviewposition)

## `insert_predefined_views`

Fill every predefined-view placeholder on a sheet via IDrawingDoc::InsertModelInPredefinedView. Predefined views are placeholders pre-positioned/pre-configured on a drawing template beforehand (Insert > Drawing View > Predefined) -- this only fills existing placeholders, it does not create them. Returns which placeholders were filled; errors clearly if the sheet has no predefined-view placeholders.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `model_path` | string | yes |  |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IDrawingDoc::InsertModelInPredefinedView`](api/02-views.md#idrawingdocinsertmodelinpredefinedview)

## `insert_auxiliary_view`

Insert an auxiliary view off an edge of an existing view via IDrawingDoc::CreateAuxiliaryViewAt2 -- unlike 'projected view,' this UI action's real API name matches. edge_selection is a sheet-space point on the reference edge (in set_units' unit), selected before the call since CreateAuxiliaryViewAt2 takes no edge parameter of its own; parent_view_name is validated against the sheet's actual views for a clear error, though the call itself operates purely off the selected edge.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `parent_view_name` | string | yes |  |
  | `edge_selection` | object | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `label` | string | no |  |
  | `flip` | boolean | no | False |
  | `not_aligned` | boolean | no | False |
  | `show_arrow` | boolean | no | True |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IDrawingDoc::CreateAuxiliaryViewAt2`](api/02-views.md#idrawingdoccreateauxiliaryviewat2)

## `insert_section_view`

Insert a section view off an existing drawing view via IDrawingDoc::CreateSectionViewAt5. Owns the whole sequence: activates parent_view_name, sketches cut_points as line segments (N-1 segments for N points -- 2 points is a straight cut, 3+ is offset/stepped) via the sketch manager in the parent view's own coordinate space, selects them, creates the view, then configures it. section_type is full (default), aligned (swCreateSectionView_OffsetSection), or half (swCreateSectionView_Partial -- SolidWorks has no true half-section flag under any name; this is the closest documented behavior, and is only valid with a straight 2-point cut). auto_hatch and display_only are applied post-creation via IDrSection::SetAutoHatch /SetDisplayOnlySurfaceCut -- display_only is NOT a true 'no material cut' toggle (none exists in this API), only the closest named setting (surface-body cut display). use_sheet_scale sets IView::UseSheetScale afterward. Section scope (excluding components/ribs from an assembly's cut) is out of scope for this tool -- SolidWorks does not pop a dialog for it during API-driven creation, since IDrSection exposes that state programmatically, so no user-preference toggle is touched. Fewer than 2 cut_points, or fewer than 2 distinct points, fails before any COM call.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `parent_view_name` | string | yes |  |
  | `cut_points` | array | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `label` | string | no |  |
  | `flip_direction` | boolean | no | False |
  | `section_type` | string | no | full |
  | `auto_hatch` | boolean | no | True |
  | `display_only` | boolean | no | False |
  | `use_sheet_scale` | boolean | no | True |

- **Dossier reference:** [`IDrawingDoc::CreateSectionViewAt5`](api/02-views.md#idrawingdoccreatesectionviewat5), [`IDrSection::SetAutoHatch`](api/02-views.md#idrsectionsetautohatch), [`IView::UseSheetScale`](api/02-views.md#iviewusesheetscale)

## `insert_detail_view`

Insert a detail view off a circular region of an existing drawing view via IDrawingDoc::CreateDetailViewAt4 (requested as CreateDetailViewAt5, which does not exist -- At4 is the current highest overload). Owns the whole sequence: activates parent_view_name, sketches the detail circle at center_x/center_y/radius via ISketchManager::CreateCircleByRadius, selects it, creates the view. style is circle (default), profile, or none -- despite the name this binds to CreateDetailViewAt4's Showtype parameter (swDetCircleShowType_e), not its separate border/leader-look Style parameter (always swDetViewSTANDARD, not exposed). scale_num/scale_denom must be given together or both omitted; omitted defaults to the parent view's own scale. A non-positive radius fails before any COM call. On any failure after the circle is sketched, it is deleted so no stray construction sketch is left on the sheet.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `parent_view_name` | string | yes |  |
  | `center_x` | number | yes |  |
  | `center_y` | number | yes |  |
  | `radius` | number | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `label` | string | no |  |
  | `scale_num` | number | no |  |
  | `scale_denom` | number | no |  |
  | `style` | string | no | circle |
  | `full_outline` | boolean | no | False |

- **Dossier reference:** [`IDrawingDoc::CreateDetailViewAt4`](api/02-views.md#idrawingdoccreatedetailviewat4), [`ISketchManager::CreateCircleByRadius`](api/02-views.md#isketchmanagercreatecirclebyradius-added-for-sw-8ww4)

## `insert_broken_out_section`

Insert a broken-out section on an existing drawing view via IDrawingDoc::CreateBreakOutSection (requested as IView::InsertBrokenOutSection, which does not exist). Owns the whole sequence: activates parent_view_name, sketches profile_points as a closed loop of line segments (auto-closed -- the last point connects back to the first), selects them, creates the section. Exactly one of depth or depth_reference is required: depth is a plain numeric depth; depth_reference selects a geometry reference (default type 'FACE') and applies it afterward via IBrokenOutSectionFeatureData::DepthReference (CreateBreakOutSection itself has no reference-depth parameter). preview=True sketches and validates the profile then deletes it without ever calling CreateBreakOutSection -- a dry run, not backed by a real COM 'preview' concept. Fewer than 3 profile_points, or fewer than 3 distinct points, fails before any COM call. On any failure after the profile is sketched, it is deleted so no stray construction sketch is left on the sheet.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `parent_view_name` | string | yes |  |
  | `profile_points` | array | yes |  |
  | `depth` | number | no |  |
  | `depth_reference` | object | no |  |
  | `preview` | boolean | no | False |

- **Dossier reference:** [`IDrawingDoc::CreateBreakOutSection`](api/02-views.md#idrawingdoccreatebreakoutsection)

## `insert_break_view`

Insert a break into an existing drawing view via IView::InsertBreak3 followed by IDrawingDoc::BreakView (requested as IDrawingDoc::InsertBreak, which does not exist -- the parameterized call lives on IView, not IDrawingDoc). InsertBreak3 only creates the break lines; BreakView (called on the selected view) is what actually applies/displays the break. position1/position2 are in the view's coordinate space (a Y value if orientation is horizontal, an X value if vertical). orientation is vertical (default) or horizontal -- anything else fails before any COM call. gap sets IView::BreakLineGap (a separate property, not an InsertBreak3 parameter); omit to leave the existing gap untouched. style is straight/zigzag (default)/curve/small_zigzag/jagged, mapped to swBreakLineStyle_e -- an unknown value fails listing the valid values. Fails without touching the view if it is already broken (IView::IsBroken) -- call remove_break_view first to re-break it -- and fails after BreakView if the view still reports not broken, since BreakView's own failure mode is a silent no-op. Returns the resulting break count.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `position1` | number | yes |  |
  | `position2` | number | yes |  |
  | `orientation` | string | no | vertical |
  | `gap` | number | no |  |
  | `style` | string | no | zigzag |

- **Dossier reference:** [`IView::InsertBreak3`](api/02-views.md#iviewinsertbreak3), [`IDrawingDoc::BreakView`](api/02-views.md#idrawingdocbreakview)

## `remove_break_view`

Remove all breaks from a drawing view via select + IDrawingDoc::UnBreakView -- there is no dedicated per-break removal call; UnBreakView acts on whichever view is currently selected. Verifies via IView::IsBroken() afterward and fails if the view still reports broken.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |

- **Dossier reference:** [`IDrawingDoc::UnBreakView`](api/02-views.md#idrawingdocunbreakview)

## `add_crop_view`

Crop an existing drawing view to a closed sketch profile via IView::Crop2 (requested as IView::CropView, which does not exist). Owns the whole sequence: activates view_name, sketches profile_points as a closed loop of line segments (auto-closed -- the last point connects back to the first), selects them, crops. Fails before any COM call if profile_points has fewer than 3 (or fewer than 3 distinct) points. Fails without sketching if the view is already cropped -- crop a view only once; call remove_crop_view first to re-crop. On any failure after the profile is sketched, it is deleted so no stray construction sketch is left on the sheet.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `profile_points` | array | yes |  |

- **Dossier reference:** [`IView::Crop2`](api/02-views.md#iviewcrop2)

## `remove_crop_view`

Remove a view's crop via select + ISldWorks::RunCommand(swCommands_Tools_Crop_Delete) (requested as IView::RemoveCropView, which does not exist and has no dedicated API equivalent -- crop removal is documented only as a right-click UI action; this fires the same command ID that action fires). Fails without calling RunCommand if the view is not currently cropped. Verifies via IView::IsCropped() afterward and fails if the view still reports cropped.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |

- **Dossier reference:** [`ISldWorks::RunCommand`](api/02-views.md#isldworksruncommand-workaround-for-removing-a-crop-view-no-dedicated-api-method-exists)

## `list_views`

Enumerate views on a drawing sheet via ISheet::GetViews -- name, type, scale, position, referenced model, and parent view. The discovery tool for addressing views by name in later view/annotation tools.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`ISheet::GetViews`](api/02-views.md#isheetgetviews)

## `move_view`

Move a drawing view to an exact sheet-space position via IView::Position. If the view is aligned to a parent view (can only move along its alignment vector, per docs/api/02-views.md), this reports the lock with swFeatureError instead of silently moving it somewhere else or no-op'ing -- call align_view with alignment='break' first to free it.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IView::Position`](api/02-views.md#iviewposition)

## `align_view`

Align a drawing view to a reference view via IView::AlignWithView, or break/reset its alignment via IView::RemoveAlignment/UseDefaultAlignment. alignment='none'/'break' is the escape hatch for move_view's alignment-lock refusal -- removes the alignment restriction so the view can be moved to an arbitrary position.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `reference_view_name` | string | no |  |
  | `alignment` | string | no | horizontal |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IView::AlignWithView`](api/02-views.md#iviewalignwithview), [`IView::RemoveAlignment`](api/02-views.md#iviewremovealignment-iviewusedefaultalignment)

## `set_view_scale`

Set a drawing view's scale independently of the sheet scale, via IView::ScaleRatio + IView::UseSheetScale. scale_num/scale_denom must be given together (and are mutually exclusive with use_sheet_scale=True); use_sheet_scale=True links the view's scale back to the sheet's own scale instead.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `scale_num` | number | no |  |
  | `scale_denom` | number | no |  |
  | `use_sheet_scale` | boolean | no | False |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IView::ScaleRatio`](api/02-views.md#iviewscaleratio), [`IView::UseSheetScale`](api/02-views.md#iviewusesheetscale)

## `set_view_display_mode`

Set a drawing view's display mode via IView::SetDisplayMode3. mode is wireframe, hidden-lines-visible, hidden-lines-removed, shaded, or shaded-with-edges. shadows maps to SetDisplayMode3's Edges parameter (edges shown when shaded); high_quality maps to the inverse of its Facetted parameter.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `mode` | string | yes |  |
  | `shadows` | boolean | no | False |
  | `high_quality` | boolean | no | True |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IView::SetDisplayMode3`](api/02-views.md#iviewsetdisplaymode3)

## `delete_view`

Delete a drawing view via select-then-IModelDocExtension::DeleteSelection2 (no dedicated DeleteView2 call exists). Refuses to delete a view with dependent child views (section/detail/projected/auxiliary views derived from it) unless cascade=True, in which case every descendant is deleted first (deepest first), then the named view -- data.removed lists every view actually removed, in deletion order.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `cascade` | boolean | no | False |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IModelDocExtension::DeleteSelection2`](api/02-views.md#imodeldocextensiondeleteselection2)

## `auto_arrange_views`

Lay out every view on a sheet with no overlapping bounding boxes, via IView::GetOutline + IView::Position -- deterministic grid/row packing (same input outlines always yield the same positions). Views are grouped by alignment root (IView::GetBaseView); only each group's root is repositioned directly, since an aligned child view can only move along its alignment vector -- SolidWorks carries aligned children along when their root moves, the same way dragging a base view in the UI drags its projected views. A view that is itself alignment-locked (IView::GetAlignment) with no GetBaseView parent -- e.g. explicitly aligned via align_view -- is left untouched and reported in data.locked rather than moved. The tool that makes a batch-generated view pack presentable.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `sheet_name` | string | no |  |
  | `margin` | number | no |  |

- **Dossier reference:** [`IView::GetOutline`](api/02-views.md#iviewgetoutline), [`IView::Position`](api/02-views.md#iviewposition), [`IView::GetBaseView`](api/02-views.md#iviewgetbaseview-iviewigetbaseview), [`IView::GetAlignment`](api/02-views.md#iviewgetalignment)

## `insert_model_items`

Import model annotations (dimensions, datums, GTols, surface finishes, weld symbols, notes, hole callouts, ...) onto a drawing view via IDrawingDoc::InsertModelAnnotations4 -- the fastest route to a fully dimensioned view for a part modeled with driving dimensions or DimXpert. Pass exactly one of view_name (a specific view) or all_views=True (every view on the active sheet, with per-view counts reported). Reports how many annotations were actually imported per view -- a zero-import result is still a warned success, not a bare 'success' with nothing to show for it. Note: center marks/centerlines are NOT importable through this tool (swInsertAnnotation_e has no bit for them) -- use the dedicated center mark/centerline tools instead.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | no |  |
  | `sources` | string | no | model |
  | `types` | array | no |  |
  | `all_views` | boolean | no | False |
  | `eliminate_duplicates` | boolean | no | True |
  | `hidden_features` | boolean | no | False |

- **Dossier reference:** [`IDrawingDoc::InsertModelAnnotations4`](api/03-annotations.md#idrawingdocinsertmodelannotations4)

## `add_dimension`

Add a drawing-only reference dimension between picked entities in a view -- the fallback for anything DimXpert/insert_model_items didn't already carry over. dimension_type: 'smart' (default, SolidWorks infers the result from what's selected), 'horizontal', 'vertical', 'radial', 'diameter', 'angular'. entities is a list of entity references in the shape list_view_entities returns. Fewer entities than the type needs (2 for horizontal/vertical/angular, 1 otherwise) fails before any SolidWorks call is made. Returns the created dimension's name (IDimension::FullName, usable as dimension_name in set_dimension_value/set_dimension_text) and its value in the current default unit.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `entities` | array | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `dimension_type` | string | no | smart |

- **Dossier reference:** [`IDimension::FullName`](api/03-annotations.md#idimensionfullname)

## `add_ordinate_dimensions`

Start a baseline/ordinate dimension chain off a datum origin via IModelDocExtension::AddOrdinateDimension. origin_entity is the datum point/edge; entities are the additional members of the group -- both in the entity-reference shape list_view_entities returns. direction: 'horizontal' (default), 'vertical', 'angular', or 'auto' (orientation inferred from the selected points).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `origin_entity` | object | yes |  |
  | `entities` | array | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `direction` | string | no | horizontal |

- **Dossier reference:** [`IModelDocExtension::AddOrdinateDimension`](api/03-annotations.md#imodeldocextensionaddordinatedimension)

## `set_dimension_value`

Set a dimension's driving value via IDimension::SetSystemValue3 (meters at the COM boundary, converted from the caller's default unit). dimension_name is IDimension::FullName, e.g. as returned by add_dimension's data.name. Fails clearly (naming the reason -- e.g. dimension driven by geometry) rather than silently no-op-ing.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `dimension_name` | string | yes |  |
  | `value` | number | yes |  |

- **Dossier reference:** [`IDimension::SetSystemValue3`](api/03-annotations.md#idimensionsetsystemvalue3), [`IDimension::FullName`](api/03-annotations.md#idimensionfullname)

## `set_dimension_text`

Set a dimension's prefix/suffix/full-override text via IDisplayDimension::SetText -- for tolerance callouts and 'TYP'/'REF' annotations. At least one of prefix/suffix/override is required. override replaces the entire text and clears the suffix/live value display (a SolidWorks behavior, not a bug here) -- combining it with suffix in the same call is not a meaningful combination.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `dimension_name` | string | yes |  |
  | `prefix` | string | no |  |
  | `suffix` | string | no |  |
  | `override` | string | no |  |

- **Dossier reference:** [`IDisplayDimension::SetText`](api/03-annotations.md#idisplaydimensionsettext)

## `autodimension_view`

Bulk-dimension a drawing view via IDrawingDoc::AutoDimension -- a 'just add reasonable baseline dimensions' fallback for a view with no usable DimXpert data. scheme: 'baseline' (default), 'ordinate', 'chain'. entities: 'all' (default), 'based_on_preselect', 'selected'. horizontal_placement: 'above' (default) or 'below'. vertical_placement: 'left' (default) or 'right'. Fails clearly (naming the reason, e.g. no dimensionable entities) rather than a silent no-op.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `scheme` | string | no | baseline |
  | `entities` | string | no | all |
  | `horizontal_placement` | string | no | above |
  | `vertical_placement` | string | no | left |

- **Dossier reference:** [`IDrawingDoc::AutoDimension`](api/03-annotations.md#idrawingdocautodimension)

## `add_note`

Add a general or flag note to a drawing sheet via IDrawingDoc::CreateText2. x/y place the note relative to the sheet's lower-left corner, caller's default unit. leader (optional) attaches a leader with a style ('none'/'straight'/'bent'/'underline') and an optional attachment point + arrow style -- see the leader schema. text accepts '\n' for multi-line notes (SolidWorks' own line-feed convention -- passed straight through, no translation needed). bold/italic wrap the text in SolidWorks' '<FONT style=B/I>' inline formatting instruction. Returns the note's SolidWorks-assigned name (data.name, e.g. 'Note1') -- pass that to edit_note to update it later.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `text` | string | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `view_name` | string | no |  |
  | `leader` | object | no |  |
  | `height` | number | no |  |
  | `angle` | number | no | 0 |
  | `bold` | boolean | no | False |
  | `italic` | boolean | no | False |
  | `layer` | string | no |  |

- **Dossier reference:** [`IDrawingDoc::CreateText2`](api/03-annotations.md#idrawingdoccreatetext2)

## `add_property_note`

Add a note whose text is entirely a linked custom-property reference -- the mechanism that keeps a title block's 'Weight'/'Material'/etc. fields live against the model, via INote::PropertyLinkedText. source='sheet' (default) emits $PRPSHEET:"property_name" (the model shown in the sheet's 'model shown in' setting -- what title blocks use for part properties); source='model' emits $PRP:"property_name" (the drawing document's own properties). prefix/suffix add literal text around the link, e.g. prefix='Weight: '. Accepts the same view_name/leader/height/angle/bold/italic/layer options as add_note.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `property_name` | string | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `source` | string | no | sheet |
  | `prefix` | string | no |  |
  | `suffix` | string | no |  |
  | `view_name` | string | no |  |
  | `leader` | object | no |  |
  | `height` | number | no |  |
  | `angle` | number | no | 0 |
  | `bold` | boolean | no | False |
  | `italic` | boolean | no | False |
  | `layer` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `list_notes`

Enumerate existing notes -- text, position, layer -- via IView::GetFirstNote/INote::GetNext, so an LLM can find (and then edit_note) a template's placeholder notes without a mouse. view_name restricts to one view's notes; sheet_name restricts to one sheet's real views plus its sheet-level/title-block notes. Omit both for every note in the whole document.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | no |  |
  | `sheet_name` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `edit_note`

Update an existing note's text and/or position -- how a caller fills in a template's placeholder notes without recreating them. note_name is IAnnotation::GetName's value (e.g. 'Note1'), as returned by add_note/add_property_note's data.name or list_notes' data.notes[i].name. Unrecognized: fails listing every note name found in the document. text accepts '\n' for multi-line, same as add_note. x/y may be given alone -- the other axis is read back from the note's current position and left unchanged.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `note_name` | string | yes |  |
  | `text` | string | no |  |
  | `x` | number | no |  |
  | `y` | number | no |  |

- **Dossier reference:** [`IAnnotation::GetName`](api/03-annotations.md#iannotationgetname-sw-1xx3)

## `list_datums`

Enumerate existing datum feature tags -- label, position, view -- via IView::GetFirstDatumTag/IDatumTag::GetNext, so add_gtol's datum-letter validation and add_datum_feature's auto-lettering have something to read. sheet_name restricts to one sheet; omit for every datum tag in the document. data.letters is the sorted, deduplicated set of labels found.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `sheet_name` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `add_datum_feature`

Place a datum feature symbol (A, B, C...) on a selected edge/face/dimension via IModelDoc2::InsertDatumTag2. label is the datum letter (up to 2 characters) -- omit to auto-assign the next unused letter A-Z, skipping the reserved letters I, O, Q (reading existing tags via list_datums). Explicitly passing I/O/Q fails. style: 'default', 'square', or 'round' (IDatumTag::SetDisplayStyle).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `entity` | object | yes |  |
  | `label` | string | no |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `style` | string | no |  |

- **Dossier reference:** [`IModelDoc2::InsertDatumTag2`](api/03-annotations.md#imodeldoc2insertdatumtag2)

## `add_gtol`

Add a geometric tolerance feature control frame via IModelDoc2::InsertGtol + IGtol::SetFrameSymbols2/SetFrameValues2. symbol is one of the 14 geometric characteristics: 'position', 'flatness', 'perpendicularity', 'parallelism', 'concentricity', 'straightness', 'circularity', 'cylindricity', 'profile_of_a_line', 'profile_of_a_surface', 'angularity', 'symmetry', 'circular_runout', 'total_runout'. datums is an ordered list of up to 3 references (primary/secondary/tertiary), each a bare letter string or {letter, modifier: 'MMC'|'LMC'|'RFS'} -- every letter must already exist on the drawing (see list_datums/add_datum_feature), and form tolerances (flatness, straightness, circularity, cylindricity) must omit datums while orientation/location/runout characteristics require at least one. material_condition modifies the tolerance value itself. projected_zone (IGtol::SetPTZHeight2) adds a projected-tolerance-zone height. leader=False creates a freestanding GTol with no selection. composite adds a second stacked frame row: {tolerance, datums, material_condition}.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `entity` | object | yes |  |
  | `symbol` | string | yes |  |
  | `tolerance` | number | yes |  |
  | `datums` | array | no |  |
  | `x` | number | no |  |
  | `y` | number | no |  |
  | `material_condition` | string | no |  |
  | `projected_zone` | number | no |  |
  | `leader` | boolean | no | True |
  | `composite` | object | no |  |

- **Dossier reference:** [`IModelDoc2::InsertGtol`](api/03-annotations.md#imodeldoc2insertgtol), [`IGtol::SetFrameSymbols2`](api/03-annotations.md#igtolsetframesymbols2-igtolsetframevalues2-legacy-pre-2022-format-frame-content)

## `add_datum_target`

Add a datum target symbol via IModelDocExtension::InsertDatumTargetSymbol3. label is the target's datum reference text (e.g. 'a1'). area_type: 'point', 'circle', or 'rectangle'. size is the target area diameter/width, caller's default unit.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `entity` | object | yes |  |
  | `label` | string | yes |  |
  | `area_type` | string | yes |  |
  | `size` | number | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |

- **Dossier reference:** [`IModelDocExtension::InsertDatumTargetSymbol3`](api/03-annotations.md#imodeldocextensioninsertdatumtargetsymbol3)

## `add_surface_finish`

Add a surface finish symbol via IModelDocExtension::InsertSurfaceFinishSymbol3. symbol_type: 'basic', 'machining_required', or 'machining_prohibited'. roughness_max/roughness_min are optional roughness values (roughness_min must be <= roughness_max if both given); machining_allowance and production_method are optional display text. lay_direction: 'circular', 'cross', 'multi_directional', 'parallel', 'perpendicular', 'radial', or 'particulate' -- omit for no lay symbol. all_around enables the all-around leader symbol (IAnnotation::SetLeader3).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `entity` | object | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `symbol_type` | string | no | basic |
  | `roughness_max` | number | no |  |
  | `roughness_min` | number | no |  |
  | `machining_allowance` | string | no |  |
  | `lay_direction` | string | no |  |
  | `production_method` | string | no |  |
  | `all_around` | boolean | no | False |

- **Dossier reference:** [`IModelDocExtension::InsertSurfaceFinishSymbol3`](api/03-annotations.md#imodeldocextensioninsertsurfacefinishsymbol3), [`IAnnotation::SetLeader3`](api/03-annotations.md#iannotationsetleader3)

## `add_weld_symbol`

Add an ISO-style weld symbol via IModelDoc2::InsertWeldSymbol3 + IWeldSymbol::SetText (SetText's Symbol only accepts ISO codes, not AWS/ANSI ones). symbol (default 'fillet') is the arrow-side weld symbol, per the ISO drafting-standard default -- a friendly alias ('fillet', 'plug', 'slot', 'spot', 'seam', 'backing', 'butt', 'square_groove', 'v_groove', 'bevel_groove', 'u_groove', 'j_groove') or a raw ISO code (BUTT, BUSQ, BUSV, BUSB, BUSVBR, BUSBR, BUSU, BUSJ, BACK, FILL, PLUG, SPOT, SEAM, SEAMC, JSPT, JSM), case-insensitive. size is displayed to the left of the symbol; length/pitch (intermittent weld -- must both be given or both omitted) are displayed to the right as 'length-pitch'. contour: 'none', 'flat', 'convex', or 'concave'. field_weld adds a field/site weld marking. all_around enables the all-around (peripheral) weld symbol. both_sides mirrors arrow-side content to the other side. other_side_symbol sets distinct other-side content (same accepted shapes as symbol). tail_text is the specification/process text in the symbol's tail.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `entity` | object | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `symbol` | string | no | fillet |
  | `size` | number | no |  |
  | `length` | number | no |  |
  | `pitch` | number | no |  |
  | `contour` | string | no |  |
  | `field_weld` | boolean | no | False |
  | `all_around` | boolean | no | False |
  | `both_sides` | boolean | no | False |
  | `other_side_symbol` | string | no |  |
  | `tail_text` | string | no |  |

- **Dossier reference:** [`IModelDoc2::InsertWeldSymbol3`](api/03-annotations.md#imodeldoc2insertweldsymbol3-iweldsymbolsettext), [`IWeldSymbol::SetText`](api/03-annotations.md#imodeldoc2insertweldsymbol3-iweldsymbolsettext)

## `add_center_marks`

Batch-apply center marks to circular holes in a drawing view via IDrawingDoc::InsertCenterMark3 -- one COM call per hole. target='all_holes' (default) finds every circular edge in the view automatically; pass a list of entity references (as returned by list_view_entities) to mark specific holes instead. A view with no circular geometry returns success with count=0, not an error. style: 'non_annotation', 'single' (default), 'linear_group', or 'circular_group'. size is the optional line length (caller's default unit); extended_lines shows extension lines (default True); slot_center_marks applies InsertCenterMark3's Slot flag to every mark (default True); connection_lines shows a circular connection line grouping the marks (default False).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `target` | any | no | all_holes |
  | `style` | string | no | single |
  | `size` | number | no |  |
  | `extended_lines` | boolean | no | True |
  | `slot_center_marks` | boolean | no | True |
  | `connection_lines` | boolean | no | False |

- **Dossier reference:** [`IDrawingDoc::InsertCenterMark3`](api/03-annotations.md#idrawingdocinsertcentermark3)

## `add_centerlines`

Insert a centerline via IDrawingDoc::InsertCenterLine2. select_view=True (default, requires target='all') selects the view object itself, letting SolidWorks auto-detect and insert centerlines wherever it can in that view. select_view=False requires target to be a list of exactly 2 entity references (as returned by list_view_entities) -- two parallel edges, or two circular/arc edges -- to draw one centerline between. InsertCenterLine2 returns a single pointer, so count is 1 if a centerline was created, 0 otherwise (not a batch total); a 0 result is a warned success, not an error.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `target` | any | no | all |
  | `select_view` | boolean | no | True |

- **Dossier reference:** [`IDrawingDoc::InsertCenterLine2`](api/03-annotations.md#idrawingdocinsertcenterline2)

## `remove_center_marks`

Remove every center mark in a drawing view (select + IModelDocExtension::DeleteSelection2), so a bad add_center_marks batch can be redone without restarting the drawing. Returns how many were actually removed; a view with none returns success with count=0.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |

- **Dossier reference:** [`IModelDocExtension::DeleteSelection2`](api/02-views.md#imodeldocextensiondeleteselection2)

## `insert_bom_table`

Insert a BOM table onto a drawing view via IView::InsertBomTable6. bom_type: 'top_level' (default), 'parts_only', or 'indented' -- 'top_level' must not be combined with configuration (use IBomFeature::GetConfigurations/SetConfigurations instead); the other two require it. Anchor mode and x/y mode are mutually exclusive: pass attach_to_anchor=True with anchor set to snap to the sheet format's BOM anchor point, or leave attach_to_anchor False (default) and use x/y -- passing anchor without attach_to_anchor=True, or attach_to_anchor=True without anchor, both fail. template_path falls back to the SolidWorks default .sldbomtbt template when omitted, erroring with swTemplateNotFound if none can be found. view_name defaults to the first view on the active sheet. Returns the created table's name, row_count, and column_count for balloon/update tools to address it by.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | no |  |
  | `template_path` | string | no |  |
  | `x` | number | no | 0 |
  | `y` | number | no | 0 |
  | `bom_type` | string | no | top_level |
  | `configuration` | string | no |  |
  | `anchor` | string | no |  |
  | `attach_to_anchor` | boolean | no | False |
  | `detailed_cut_list` | boolean | no | False |
  | `hidden_columns` | array | no |  |

- **Dossier reference:** [`IView::InsertBomTable6`](api/04-tables.md#iviewinsertbomtable6)

## `list_tables`

Enumerate every table annotation (BOM, hole, revision, weldment cut list, general, title block, ...) via IView::GetFirstTableAnnotation/ITableAnnotation::GetNext. sheet_name restricts to one sheet's tables (its real views plus its sheet-level/title-block table); omitted: every table in the whole document. Each record has type, name, position (x/y), view_name, and row_count/column_count (a table's size -- tables have no overall width/height property).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `sheet_name` | string | no |  |

- **Dossier reference:** [`IView::GetFirstTableAnnotation`](api/04-tables.md#iviewgetfirsttableannotation)

## `get_bom_contents`

Read a BOM table's cell text back via ITableAnnotation::Text2, so an LLM can verify an assembly drawing's BOM without opening SolidWorks. table_name is IAnnotation::GetName's value, as returned by insert_bom_table's data.name or list_tables' data.tables[i].name. Returns rows (including the header row at index 0), each row_count x column_count cell strings. Fails if no table has that name, or if it isn't a BOM table.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `table_name` | string | yes |  |

- **Dossier reference:** [`ITableAnnotation::Text2`](api/04-tables.md#itableannotationtext2), [`IAnnotation::GetName`](api/03-annotations.md#iannotationgetname-sw-1xx3)

## `auto_balloon_view`

Auto-balloon an entire drawing view via IDrawingDoc::CreateAutoBalloonOptions + AutoBalloon5 -- the common case; use add_balloon for the one-off single-balloon case instead. layout: 'square' (default), 'circle', 'top', 'bottom', 'right', or 'left'. style: 'circular' (default), 'triangle', 'hexagon', 'box', 'diamond', 'pentagon', 'split_circle', 'flag_pentagon', 'flag_triangle', 'underline', 'square', 's_circle', 'inspection', 'arc_bracket', 'rect_bracket', 'arc_length_symbol', 'fixed_symbol', 'double_arrow', 'split_square', 'verbose', or 'none'. size: 'tight_fit' (default), '1_char', '2_chars', '3_chars', '4_chars', or '5_chars'. text_content: 'item_number' (default), 'custom', 'quantity', 'custom_properties', 'component_reference', 'spool_reference', 'part_number_bom', 'file_name', 'cutlist_properties', 'view_sheet', 'view_sheet_with_label', 'view_zone', or 'view_letter'. leader_attachment: 'edge' (default) or 'face'. bom_table_name, if given, requires a BOM table with exactly that name to already exist on the view's sheet, failing before any COM call otherwise; omitted, any BOM table on the sheet avoids the missing-BOM warning, and no BOM table at all still runs (balloons are still created) but the result message warns that item numbers may be meaningless. Returns the number of balloons created in data.count.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `layout` | string | no | square |
  | `style` | string | no | circular |
  | `size` | string | no | tight_fit |
  | `text_content` | string | no | item_number |
  | `reverse_direction` | boolean | no | False |
  | `ignore_multiple` | boolean | no | True |
  | `insert_magnetic_line` | boolean | no | False |
  | `leader_attachment` | string | no | edge |
  | `bom_table_name` | string | no |  |

- **Dossier reference:** [`IDrawingDoc::CreateAutoBalloonOptions`](api/04-tables.md#idrawingdoccreateautoballoonoptions)

## `add_balloon`

Add a single BOM balloon via IModelDocExtension::InsertBOMBalloon -- the one-off case; use auto_balloon_view to balloon a whole view instead. entity is the item to balloon (typically kind='component' for an assembly component instance, or an edge/face/vertex as returned by list_view_entities). x/y place the balloon, caller's default unit. style/size/text_content use the same values as auto_balloon_view. lower_text is only valid when style='split_circle'. upper_text is only used when text_content='custom'. quantity_display shows the item quantity on the balloon.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `entity` | object | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `style` | string | no | circular |
  | `size` | string | no | tight_fit |
  | `text_content` | string | no | item_number |
  | `upper_text` | string | no |  |
  | `lower_text` | string | no |  |
  | `quantity_display` | boolean | no | False |

- **Dossier reference:** [`IModelDocExtension::InsertBOMBalloon`](api/04-tables.md#imodeldocextensioninsertbomballoon)

## `renumber_balloons`

Deterministically renumber every BOM balloon via INote::SetBomBalloonText. view_name restricts to one view's balloons; omitted, every BOM balloon in the document. start is the first item number (default 1). order='by_position' (the only implemented order) sorts top-left first: descending sheet Y (top first), then ascending sheet X (left first), then balloon name as a stable tie-break -- so the same balloon positions always produce the same numbering. Returns the renumbered count in data.count and each balloon's assigned number in data.balloons.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | no |  |
  | `start` | integer | no | 1 |
  | `order` | string | no | by_position |

- **Dossier reference:** [`INote::SetBomBalloonText`](api/04-tables.md#inotesetbomballoontext)

## `remove_balloons`

Clear every BOM balloon note from a drawing view via INote::IsBomBalloon + IModelDocExtension::DeleteSelection2 -- lets a bad auto_balloon_view/add_balloon batch be redone without restarting the drawing. Returns the removed count in data.count; a view with no balloons is a warned success with count 0.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |

- **Dossier reference:** [`INote::IsBomBalloon`](api/04-tables.md#inoteisbomballoon), [`IModelDocExtension::DeleteSelection2`](api/02-views.md#imodeldocextensiondeleteselection2)

## `insert_hole_table`

Insert a hole table onto a drawing view via IView::InsertHoleTable3, after atomically selecting datum_entity (Mark=1) -- a hole table's X/Y columns are relative to a pre-selected datum origin vertex or edge. tag_style: 'alphanumeric' (default) or 'numeric'. combine_same_size: True (default) merges cells of same-size holes via IHoleTable::CombineSameSize. template_path falls back to the SolidWorks default .sldholtbt template when omitted, erroring with swTemplateNotFound if none can be found. Returns the created table's name, row_count, and column_count.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `datum_entity` | object | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `template_path` | string | no |  |
  | `tag_style` | string | no | alphanumeric |
  | `combine_same_size` | boolean | no | True |

- **Dossier reference:** [`IView::InsertHoleTable3`](api/04-tables.md#iviewinsertholetable3), [`IHoleTable::CombineSameSize`](api/04-tables.md#iholetablecombinesamesize)

## `insert_revision_table`

Insert a revision table onto the active sheet via ISheet::InsertRevisionTable2 (requested as IDrawingDoc::InsertRevisionTable2, which does not exist). anchor=True (default) inserts at the sheet's existing revision-table anchor point and x/y must be omitted; anchor=False requires x/y. alpha_numeric is accepted and echoed for forward compatibility -- SolidWorks has no per-table alpha/numeric COM property; add_revision infers the numbering scheme from the table's existing rows instead. symbol_shape: 'circle' (default), 'square', 'triangle', or 'hexagon'. template_path falls back to the SolidWorks default .sldrevtbt template when omitted, erroring with swTemplateNotFound if none can be found, and with swFeatureError if the sheet already has a revision table (only one is allowed per sheet). Returns the created table's name, row_count, and column_count.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `x` | number | no |  |
  | `y` | number | no |  |
  | `template_path` | string | no |  |
  | `anchor` | boolean | no | True |
  | `alpha_numeric` | boolean | no | True |
  | `symbol_shape` | string | no | circle |

- **Dossier reference:** [`ISheet::InsertRevisionTable2`](api/04-tables.md#isheetinsertrevisiontable2)

## `add_revision`

Append a row to the document's revision table via IRevisionTableAnnotation::AddRevision. revision omitted: auto-incremented from the table's last row (A -> B, 1 -> 2; a brand-new table with no rows starts at 'A'). date omitted: defaults to today, formatted MM/DD/YY. approved_by/zone are written only if the table's template has a matching column (silently skipped otherwise, reported in data.skipped_fields). Fails with swInvalidInput if no revision table exists yet -- call insert_revision_table first.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `description` | string | yes |  |
  | `revision` | string | no |  |
  | `date` | string | no |  |
  | `approved_by` | string | no |  |
  | `zone` | string | no |  |

- **Dossier reference:** [`IRevisionTableAnnotation::AddRevision`](api/04-tables.md#irevisiontableannotationaddrevision)

## `insert_weldment_cutlist`

Insert a weldment cut list table onto a drawing view via IView::InsertWeldmentTable (requested as IModelDocExtension::InsertWeldmentCutlist, which does not exist under that name or any variant spelling). template_path falls back to the SolidWorks default .sldwldtbt template when omitted, erroring with swTemplateNotFound if none can be found. Fails with swFeatureError, distinctly from an empty table, when the view's referenced model has no weldment cut list feature (detected via IWeldmentCutListAnnotation::WeldmentCutListFeature reading back empty). Returns the created table's name, row_count, and column_count.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `view_name` | string | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |
  | `template_path` | string | no |  |

- **Dossier reference:** [`IView::InsertWeldmentTable`](api/04-tables.md#iviewinsertweldmenttable), [`IWeldmentCutListAnnotation::WeldmentCutListFeature`](api/04-tables.md#iweldmentcutlistannotationweldmentcutlistfeature)

## `update_table`

Force a table (or every table on the active sheet) to reflect the document's current state -- via IModelDoc2::ForceRebuild3 plus a per-table IAnnotation::Visible toggle; there is no ITableAnnotation::Update (see docs/api/04-tables.md's intro). Pass exactly one of table_name or all_tables=True. table_name updates one table, searched across the whole document. all_tables=True updates every table on the active sheet and reports each one in data.tables, each with its own refreshed flag (True only if that table's Visible toggle actually ran; an already-hidden table is left alone and reported as not refreshed rather than counted as updated) -- data.refreshed_count totals it. An empty sheet is a warned success with count 0.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `table_name` | string | no |  |
  | `all_tables` | boolean | no | False |

- **Dossier reference:** [`IModelDoc2::ForceRebuild3`](api/01-documents-and-sheets.md#imodeldoc2forcerebuild3), [`IAnnotation::Visible`](api/04-tables.md#iannotationvisible)

## `get_table_contents`

Read any table's cell text back via ITableAnnotation::Text2 -- the generic counterpart of get_bom_contents, working for BOM, hole, revision, weldment cut list, general, and title block tables alike (no table-type restriction). table_name is IAnnotation::GetName's value, as returned by any insert_*_table tool's data.name or list_tables' data.tables[i].name. Returns rows (including the header row at index 0), each row_count x column_count cell strings, plus the table's type. Fails if no table has that name.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `table_name` | string | yes |  |

- **Dossier reference:** [`ITableAnnotation::Text2`](api/04-tables.md#itableannotationtext2), [`IAnnotation::GetName`](api/03-annotations.md#iannotationgetname-sw-1xx3)

## `set_table_cell`

Overwrite one table cell's driving text via ITableAnnotation::Text, refusing the write with swInvalidInput if IsCellTextEditable reports the cell read-only (e.g. an auto-generated hole table column) rather than silently no-oping. row/column are 0-based, matching Text2/IsCellTextEditable's own indexing -- out-of-range indices are rejected with swInvalidInput before any write. After writing, the cell is read back via Text2 into data.verified: a read-back that disagrees fails with swFeatureError instead of a false success, while a read-back that itself errors is not treated as a failed write -- it succeeds with data.verified=False and the read-back error named in the message.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `table_name` | string | yes |  |
  | `row` | integer | yes |  |
  | `column` | integer | yes |  |
  | `text` | string | yes |  |

- **Dossier reference:** [`ITableAnnotation::Text`](api/04-tables.md#itableannotationtext)

## `set_table_position`

Move a table to an explicit sheet-space position via the base IAnnotation::SetPosition (ITableAnnotation has no SetPosition of its own). Fails with swInvalidInput if the table is currently anchored (ITableAnnotation::Anchored=True) -- an anchored table's origin snaps back to the sheet anchor point and would immediately override an explicit position -- call set_table_anchor with anchored=False first.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `table_name` | string | yes |  |
  | `x` | number | yes |  |
  | `y` | number | yes |  |

- **Dossier reference:** [`IAnnotation::SetPosition`](api/04-tables.md#iannotationsetposition), [`ITableAnnotation::Anchored`](api/04-tables.md#itableannotationanchored)

## `set_table_anchor`

Set a table's anchored state via ITableAnnotation::Anchored. anchored=True (default) snaps the table to its type's sheet anchor point; anchored=False releases it for explicit set_table_position control. Setting anchored=True when the sheet format has no anchor point for this table's type has no effect at the COM layer -- this reads Anchored back afterward and fails with swFeatureError on a mismatch rather than a false success.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `table_name` | string | yes |  |
  | `anchored` | boolean | no | True |

- **Dossier reference:** [`ITableAnnotation::Anchored`](api/04-tables.md#itableannotationanchored)

## `delete_table`

Delete a table annotation via select (Type='ANNOTATIONTABLES') + IModelDocExtension::DeleteSelection2 -- there is no ITableAnnotation::DeleteTable, so this uses the same select-then-delete idiom remove_center_marks/remove_balloons use for annotation types with no dedicated per-object delete method. table_name is IAnnotation::GetName's value, searched across the whole document before any selection is attempted -- an unknown name fails with swInvalidInput and no COM call.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `table_name` | string | yes |  |

- **Dossier reference:** [`IModelDocExtension::DeleteSelection2`](api/02-views.md#imodeldocextensiondeleteselection2), [`IAnnotation::GetName`](api/03-annotations.md#iannotationgetname-sw-1xx3)

## `create_layer`

Create a new drawing layer via ILayerMgr::AddLayer, so generated dimensions/notes/reference geometry can be put on a named layer a drafting-standard or certification review can toggle independently. Fails with a clear error if name already exists (checked before any mutating call). visible/printable are applied after creation, visible first (setting Visible can change Printable as a side effect, so printable is re-applied second), then read back -- they come back null if the new layer could not be re-resolved, meaning those two writes never happened.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `name` | string | yes |  |
  | `description` | string | no |  |
  | `color` | any | no |  |
  | `style` | string | no |  |
  | `width` | string | no |  |
  | `visible` | boolean | no | True |
  | `printable` | boolean | no | True |

- **Dossier reference:** [`ILayerMgr::AddLayer`](api/05-export-and-layers.md#ilayermgraddlayer)

## `list_layers`

Enumerate every layer in the active drawing via ILayerMgr::GetLayerList/GetLayer. Each record has name, description, visible, printable, color ('#RRGGBB'), style, and width.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** [`ILayerMgr::GetLayerList`](api/05-export-and-layers.md#ilayermgrgetlayerlist)

## `set_current_layer`

Make an existing layer the active one via ILayerMgr::SetCurrentLayer, so subsequently created annotations land on it. Fails with swInvalidInput listing every existing layer if name does not match one.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `name` | string | yes |  |

- **Dossier reference:** [`ILayerMgr::SetCurrentLayer`](api/05-export-and-layers.md#ilayermgrsetcurrentlayer)

## `set_layer_properties`

Partially update an existing layer's ILayer properties -- every argument left unset keeps that field exactly as it already was. printable is applied after visible (setting Visible can change Printable as a side effect, so an explicit printable always wins). Fails with swInvalidInput listing every existing layer if name does not match one. Returns the layer's full state after every requested change.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `name` | string | yes |  |
  | `visible` | boolean | no |  |
  | `printable` | boolean | no |  |
  | `color` | any | no |  |
  | `style` | string | no |  |
  | `width` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `move_annotations_to_layer`

Bulk-assign existing annotations to a layer via IAnnotation::Layer -- what makes a generated dimension/note/GD&T pack reviewable by a drafting-standard pass that toggles layers. view_name restricts to one view (omitted: every view in the document). annotation_types restricts to a subset of 'note', 'datum_tag', 'table', 'dimension' (omitted: all four) -- GTols and weld symbols have no per-view enumeration in this tool layer yet, so are not movable by this tool. Fails with swInvalidInput listing every existing layer if layer_name does not match one. Reports how many annotations were moved, per type.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `layer_name` | string | yes |  |
  | `view_name` | string | no |  |
  | `annotation_types` | array | no |  |

- **Dossier reference:** [`IAnnotation::Layer`](api/03-annotations.md#iannotationlayer)

## `set_line_format`

Set line weight/style/color, either as a document-wide drafting-standard default for a named entity class (visible/hidden/section/detail_circle/dimension/construction, via IModelDocExtension::SetUserPreferenceInteger -- weight/style only, color is rejected) or as a per-entity override on an explicit list of entity references (via IDrawingDoc::SetLineWidth/SetLineStyle/SetLineColor -- all three supported). view_name only applies to the entity-list form: it activates that view first. At least one of weight/style/color must be given. For the entity-list form, data.applied counts entities the calls were sent to, not entities SolidWorks confirmed it restyled -- those three methods return no status, so an unrecognized style name is silently ignored.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `target` | any | yes |  |
  | `weight` | string | no |  |
  | `style` | string | no |  |
  | `color` | any | no |  |
  | `view_name` | string | no |  |

- **Dossier reference:** [`IModelDocExtension::SetUserPreferenceInteger`](api/05-export-and-layers.md#imodeldocextensionsetuserpreferenceinteger), [`IDrawingDoc::SetLineWidth`](api/05-export-and-layers.md#idrawingdocsetlinewidth)

## `get_line_format`

Read back the current weight/style for a named drafting-standard entity class's document default (IModelDocExtension::GetUserPreferenceInteger). color is always null -- no such document property exists. Only a named entity class is accepted, not an explicit entity list -- SolidWorks has no documented per-entity line-format read-back API.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `target` | string | yes |  |
  | `view_name` | string | no |  |

- **Dossier reference:** [`IModelDocExtension::GetUserPreferenceInteger`](api/05-export-and-layers.md#imodeldocextensiongetuserpreferenceinteger)

## `apply_drafting_standard`

Read a JSON file mapping entity classes (visible/hidden/section/detail_circle/dimension/construction) to {weight, style} and apply every entry via set_line_format in one call -- see docs/drafting_standard.example.json for the expected shape. Fails with swInvalidInput (before any COM call) naming the bad key if the file isn't valid JSON, isn't a non-empty object, or contains an unrecognized entity class or property. data.results reports each entity class's own set_line_format outcome.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `standard_file` | string | yes |  |

- **Dossier reference:** none found in `docs/api/`

## `create_drawing_pack`

Build a whole drawing (sheets, views, annotations, tables, balloons, custom properties, export) from one declarative PackSpec, instead of an LLM orchestrating 15-20 raw tool calls per part. Validates the spec (solidworks_mcp/pack/spec.py's PackSpec.validate() -- structural/referential checks only, no COM), compiles it to an ordered step list (solidworks_mcp.pack.compiler.compile), then runs each step through this same tool registry. Enforces per-sheet phase order: create sheet -> set sheet properties -> insert views (parents before children) -> auto-arrange -> REBUILD -> annotations -> insert tables -> REBUILD -> update tables -> balloons -> set custom properties; export (save_drawing to PackSpec.output) runs once at the end. The two rebuilds matter: dimension values, BOM quantities, and balloon numbers are stale until a rebuild runs. on_error='abort' (default) stops at the first failed step; 'continue' runs every remaining step and reports every failure. Either way, the full per-step log is returned so a partial pack is diagnosable. dry_run=True compiles and returns the step list (data.steps) without executing anything. Two known limitations, both from what PackSpec (solidworks_mcp/pack/spec.py) can express: the first sheet's declared name is applied via a rename right after creation, since the template's own first sheet is rarely already named that -- this is automatic and needs no action from the caller. A 'hole' table has no spec field for its required datum origin, so one is always inserted anchored at the view origin (0, 0); a real per-hole-table datum still requires editing the table afterward. Note that the auto-arrange step repositions every view and so overwrites the x/y each view declares; set a sheet's auto_arrange to false to keep the declared placement (and to keep sheet-space annotation coordinates aligned with the views they point at).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `spec` | object | yes |  |
  | `on_error` | string | no | abort |
  | `dry_run` | boolean | no | False |

- **Dossier reference:** none found in `docs/api/`

## `create_sketch`

Create a new sketch on a plane.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `plane` | string | no | Front |

- **Dossier reference:** none found in `docs/api/`

## `create_sketch_on_face`

Create a new sketch on an existing body face (by 3D coordinates). Use for cut-extrude on faces instead of reference planes.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `x` | number | no | 0 |
  | `y` | number | no | 0 |
  | `z` | number | no | 0 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `draw_line`

Draw a line in the active sketch.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `x1` | number | no | 0 |
  | `y1` | number | no | 0 |
  | `x2` | number | no | 100 |
  | `y2` | number | no | 0 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `draw_circle`

Draw a circle in the active sketch.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `x` | number | no | 0 |
  | `y` | number | no | 0 |
  | `radius` | number | no | 25 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `draw_rectangle`

Draw a rectangle in the active sketch.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `x1` | number | no | -50 |
  | `y1` | number | no | -25 |
  | `x2` | number | no | 50 |
  | `y2` | number | no | 25 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `draw_arc`

Draw an arc by center and angles.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `cx` | number | no | 0 |
  | `cy` | number | no | 0 |
  | `radius` | number | no | 25 |
  | `start_angle` | number | no | 0 |
  | `end_angle` | number | no | 90 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `draw_polygon`

Draw a regular polygon.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `cx` | number | no | 0 |
  | `cy` | number | no | 0 |
  | `radius` | number | no | 25 |
  | `sides` | integer | no | 6 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `close_sketch`

Close/exit the active sketch. Call this before extrude if sketch is still open.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `get_sketch_status`

Get diagnostic info: active sketch state, sketch count, sketch names in feature tree.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `extrude_sketch`

Extrude the active sketch (Boss-Extrude).

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `depth` | number | no | 10 |
  | `both_directions` | boolean | no | False |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `cut_extrude`

Cut extrude to remove material.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `depth` | number | no | 10 |
  | `through_all` | boolean | no | False |
  | `both_directions` | boolean | no | False |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `fillet_edges`

Add fillet to selected edges.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `radius` | number | no | 2 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `chamfer_edges`

Add chamfer to selected edges.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `distance` | number | no | 2 |
  | `angle` | number | no | 45 |
  | `unit` | string | no |  |

- **Dossier reference:** none found in `docs/api/`

## `list_features`

List all features in the model.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:** none
- **Dossier reference:** none found in `docs/api/`

## `set_units`

Set default unit for dimensions.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `unit` | string | yes |  |

- **Dossier reference:** none found in `docs/api/`

## `execute_python`

Execute custom Python code with stdout capture. Access 'sw' (app), 'doc' (active document). Use print() for debug output.

- **Minimum release:** SOLIDWORKS 2025
- **Parameters:**

  | Name | Type | Required | Default |
  | --- | --- | --- | --- |
  | `code` | string | yes |  |

- **Dossier reference:** none found in `docs/api/`
